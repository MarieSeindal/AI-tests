public class Power {

/*
    public static void main(String[] args) {
        int base = 5;
        int originalBase = base;
        int exponent =3;

        int p = power(base,exponent,originalBase);
        System.out.println(p);

    }

    public static int power(int base, int exponent,int originalBase){
        if (exponent==1)
            return base;
        base = base*base;
        exponent= exponent-1;
        power(base,exponent);
        return base;
    }
*/
}
